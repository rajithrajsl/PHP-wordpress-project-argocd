apiVersion: apps/v1
kind: Deployment
metadata:
  name: PHP-wordpress
  namespace: wordpress
spec:
  replicas: 2
  selector:
    matchLabels:
      app: PHP-wordpress
  template:
    metadata:
      labels:
        app: PHP-wordpress
    spec:
      containers:
      - name: httpd
        image: mightyleo/wordpress-new:1.0-23
        ports:
        - containerPort: 80

---

apiVersion: v1
kind: Service
metadata:
  name: PHP-wordpress
  namespace: wordpress
spec:
  selector:
    app: PHP-wordpress
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: LoadBalancer

---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: PHP-wordpress
  namespace: wordpress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - PHP-wordpress.dotnet.displayme.net  # Your domain
    secretName: dotnet.displayme.net-ssl
  rules:
  - host: PHP-wordpress.dotnet.displayme.net
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: PHP-wordpress
            port:
              number: 80
