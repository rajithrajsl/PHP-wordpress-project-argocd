apiVersion: apps/v1
kind: Deployment
metadata:
  name: wordpress-apache
  namespace: wordpress
spec:
  replicas: 2
  selector:
    matchLabels:
      app: wordpress-apache
  template:
    metadata:
      labels:
        app: wordpress-apache
    spec:
      containers:
      - name: httpd
        image: mightyleo/wordpress-new:1.0-23
        ports:
        - containerPort: 80

---

apiVersion: v1
kind: Service
metadata:
  name: wordpress-apache
  namespace: wordpress
spec:
  selector:
    app: wordpress-apache
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: LoadBalancer

---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wordpress-apache
  namespace: wordpress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - wordpress-apache.dotnet.displayme.net  # Your domain
    secretName: dotnet.displayme.net-ssl
  rules:
  - host: wordpress-apache.dotnet.displayme.net
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: wordpress-apache
            port:
              number: 80
