apiVersion: apps/v1
kind: Deployment
metadata:
  name: httpd
  namespace: stms-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: httpd
  template:
    metadata:
      labels:
        app: httpd
    spec:
      containers:
      - name: httpd
        image: mightyleo/wordpress-new:1.0-23
        ports:
        - containerPort: 80

---

apiVersion: v1
kind: Service
metadata:
  name: httpd
  namespace: stms-dev
spec:
  selector:
    app: httpd
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: LoadBalancer

---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: httpd
  namespace: stms-dev
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - apache-test.dotnet.displayme.net  # Your domain
    secretName: dotnet.displayme.net-ssl
  rules:
  - host: apache-test.dotnet.displayme.net
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: httpd
            port:
              number: 80
